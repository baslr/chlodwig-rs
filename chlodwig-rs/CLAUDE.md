# Chlodwig RS — Development Guide

## TDD: Test-Driven Development (STRICT)

This project follows **strict TDD**. Every code change MUST follow this cycle:

1. **Write the test FIRST** — before writing any implementation code
2. **Run the test** — confirm it fails (red)
3. **Write the minimal implementation** to make the test pass (green)
4. **Refactor** — clean up while keeping tests green
5. **Run `cargo t`** (alias for `cargo test --release --workspace`) — all tests must pass before committing

### Rules

- **Never skip tests.** Every new function, method, or behavior MUST have a corresponding test.
- **Never write implementation code without a failing test first.**
- **Bug fixes require a regression test** that reproduces the bug before writing the fix.
- **Edge cases are not optional.** UTF-8 boundaries, empty inputs, overflow, large data — test them.
- **Tests must be self-documenting.** Name them `test_<what_it_does>` and add a comment if the intent is not obvious.
- **Run `cargo t` after every change.** No exceptions. (Alias for `cargo test --release --workspace` — debug builds are forbidden, see "Commands" below.)

### Test Locations

| Crate | Test Location | Focus |
|-------|--------------|-------|
| chlodwig-core | `auto_scroll.rs` mod tests | AutoScroll state machine (shared by TUI+GTK) |
| chlodwig-core | `conversation.rs` mod tests | Conversation loop, compaction, SSE events |
| chlodwig-core | `messages.rs` mod tests | Serde round-trips |
| chlodwig-api | `client.rs` mod tests | SSE parser |
| chlodwig-tools | Each tool file mod tests | Tool behavior, edge cases, network (WebFetch/WebSearch) |
| chlodwig-tui | `lib.rs` mod tests | Scroll logic, input handling, UTF-8 |

## Architecture

Cargo workspace with 5 crates:

```
chlodwig-cli → chlodwig-core
             → chlodwig-api  → chlodwig-core
             → chlodwig-tools → chlodwig-core
             → chlodwig-tui   → chlodwig-core
```

`api`, `tools`, and `tui` are independent of each other. Only `cli` ties everything together.

## Key Patterns

- **Channel-based UI decoupling**: `mpsc::UnboundedSender<ConversationEvent>` from conversation loop to UI
- **Trait-based abstraction**: `ApiClient`, `Tool`, `PermissionPrompter` — all mockable in tests
- **Index-based block accumulation**: `HashMap<u32, BlockAcc>` for interleaved SSE content blocks
- **Char vs Byte distinction**: `cursor` is a char index; always use `cursor_byte_pos()` for String operations
- **Lazy redraw**: Only redraw when `needs_redraw` is set — never redraw in idle

## Known Gotchas (Learned from Bugs)

These were discovered during development. **Never regress on these:**

1. **UTF-8 input handling**: `String::insert()`/`remove()` take byte indices, not char indices. Always convert via `cursor_byte_pos()`. Tested by `test_insert_umlaut_then_ascii`, `test_insert_emoji_then_ascii`, etc.

2. **Interleaved SSE blocks**: GitHub API sends multiple `ContentBlockStart` events before any `ContentBlockStop`. Must use per-index `HashMap<u32, BlockAcc>`, not a single accumulator. Tested by `test_interleaved_tool_blocks`.

3. **`[DONE]` SSE terminator**: GitHub API sends `data: [DONE]` as stream terminator. Must filter before JSON parse. Handled in `chlodwig-api` SSE parser.

4. **Mouse move events**: `EnableMouseCapture` sends `MouseEventKind::Moved` on every mouse movement. These MUST NOT trigger redraw, or CPU goes to 100%.

5. **Scroll anchoring**: When leaving auto-scroll, anchor `scroll` at the current bottom position first, not at 0. Otherwise scroll jumps to top.

6. **Empty tool names**: Filter `tool_uses.retain(|t| !t.name.is_empty())` before executing tools. Interleaved blocks can produce empty-name entries.

7. **Tracing crate names**: Rust crate names use underscores (`chlodwig_core`), not hyphens (`chlodwig-core`). Filter expressions must match.

8. **Trackpad event flooding**: Trackpad scroll gestures generate hundreds of `MouseEventKind::Moved`/`Drag` events per gesture. If you read only ONE event per `poll()` call, the event queue never drains, `poll()` returns immediately → busy-loop at 100% CPU. **Fix**: inner drain loop that reads ALL pending events before continuing: `loop { read(); if !poll(0ms) { break; } }`. Only `ScrollUp`/`ScrollDown`/`Key`/`Resize` set `needs_redraw`; all other mouse events are silently discarded.

9. **Ghost artifacts when scrolling**: ratatui's diff-based rendering compares the current buffer against the previous frame and only sends changed cells to the terminal. **Fix**: `render_output()` pads every line with spaces to fill the full viewport width (so every cell is overwritten), and vertically pads with empty lines to fill the view height. This ensures every cell gets a value on each frame, so ratatui's diff correctly updates the entire screen. **Do NOT call `terminal.swap_buffers()` before `terminal.draw()`** — `draw()` already calls `swap_buffers()` internally (ratatui 0.29). Calling it twice corrupts the double-buffer state and produces garbled output (the "previous" buffer that ratatui diffs against becomes a stale/reset buffer).

10. **`continue` inside inner drain loop blocks**: The inner event drain loop (`loop { read(); if !poll(0ms) { break; } }`) must never use `continue` — it skips the `poll(0ms)` break-check and `read()` blocks indefinitely waiting for the next event. Always use `break` to exit the inner loop.

11. **`chars().count()` ≠ terminal display width**: Never use `chars().count()` for terminal column calculations. CJK characters, emojis, etc. occupy 2 terminal columns but are 1 char. Use `unicode_width::UnicodeWidthStr::width()` or ratatui's `Line::width()`. The `RenderedLine::display_width()` method and `wrap()` both use unicode-width. Tested by `test_display_width_uses_unicode_width`, `test_wrap_respects_unicode_display_width`.

12. **Bash commands need PTY for color output**: `std::process::Command::output()` captures stdout/stderr via pipes. Programs detect `isatty(stdout) == false` and disable ANSI colors. **Fix**: Wrap execution in `script -q /dev/null bash -c '...'` (macOS) or `script -qc '...' /dev/null` (Linux) to allocate a pseudo-terminal. Side effect: stderr is merged into stdout through the PTY, so separate stderr capture is lost. The ANSI codes are parsed by ratatui's `into_text()` in the `BashOutput` display block renderer. Tested by `test_pty_provides_isatty`.

13. **crossterm Key Release events cause double-fire**: crossterm 0.26+ sends `KeyEventKind::Press`, `KeyEventKind::Release`, and `KeyEventKind::Repeat` events. If the event loop handles ALL key events without filtering, every keystroke fires twice (press + release). **Fix**: Match only `Event::Key(key) if key.kind == KeyEventKind::Press`. Without this filter, Backspace deletes 2 chars per press, Char input inserts 2 chars, etc. Tested by `test_backspace_deletes_exactly_one_char`.

14. **Incoming content must not override manual scroll**: When the user has scrolled up (`auto_scroll=false`), streaming `TextDelta`, `ToolUseStart`, `ToolResult`, `CompactionStarted`, `CompactionComplete` events must NOT call `scroll_to_bottom()` — that forces the viewport back to the bottom, yanking the user away from what they're reading. **Fix**: Use `scroll_to_bottom_if_auto()` for all incoming-content events; it's a no-op when the user has manually scrolled. Use `scroll_to_bottom()` only for explicit user actions (submit prompt, `/compact` command, `!` shell). Tested by `test_new_content_does_not_override_manual_scroll`, `test_new_content_follows_when_auto_scroll_active`.

15. **Crash diagnostics: static 10 MiB buffer + signal handlers**: The Rust panic hook only catches `panic!()` calls. Real crashes (SIGSEGV, SIGBUS, SIGABRT, SIGFPE) bypass it entirely. **Fix**: A 10 MiB static buffer (`CRASH_BUF`) is pre-allocated in BSS at startup. Signal handlers for fatal signals restore the terminal, write crash info (backtrace + App state) into the static buffer (no heap allocation needed), flush to `crash_*.log`, dump to stderr, then re-raise with default handler. **Do NOT handle SIGINT** — crossterm captures Ctrl+C as a KeyEvent for graceful shutdown; intercepting SIGINT breaks terminal restore. The `CRASH_STATE` mutex holds the latest `App::crash_dump()` snapshot, updated every redraw (~10Hz). A `TerminalGuard` (Drop) ensures `disable_raw_mode()`+`LeaveAlternateScreen` run even when `?` operators bail out of the event loop. Tests that touch the static buffer are serialized via `SERIAL_LOCK` to avoid race conditions. Tested by `test_static_crash_buffer_write_and_read`, `test_static_crash_buffer_contains_backtrace`, `test_static_buf_size_is_10mb`.

16. **ToolResult preview byte-slicing panic**: `&t[..500]` panics when byte 500 lands inside a multi-byte UTF-8 character (e.g. `├` is 3 bytes E2 94 9C). **This was the production crash bug** — `cargo tree` output contains box-drawing characters. **Fix**: Walk backwards from byte 500 with `is_char_boundary()` to find the last valid boundary. Same pattern for `crash_dump()` streaming_buffer tail. **Never use `&str[..N]` with a hardcoded byte offset.** Always use `is_char_boundary()`, `char_indices()`, or `.chars().take(N)`. Tested by `test_tool_result_preview_truncation_at_utf8_boundary`, `test_tool_result_preview_emoji_boundary`.

17. **SIGHUP causes silent death**: Without a SIGHUP handler, the process terminates silently with no crash log when the controlling terminal sends SIGHUP (e.g. SSH disconnect, terminal multiplexer detach, or macOS terminal session cleanup). The default handler just kills the process — no panic hook, no signal handler, no log. **Fix**: Register SIGHUP alongside the other fatal signals (SIGSEGV, SIGBUS, SIGABRT, SIGFPE) so it writes a full crash report before terminating. Tested by `test_sighup_is_in_signal_handler_list`, `test_sighup_crash_report_contains_signal_name`.

18. **Eager `rebuild_requests_lines()` causes 100% CPU**: The Requests tab renders every SSE chunk as pretty-printed JSON with syntect syntax highlighting (`render_markdown` → regex DFA). Calling `rebuild_requests_lines()` eagerly on every `HttpRequest`, `HttpResponseMeta`, and `HttpResponseComplete` event creates an O(n²) loop — each new event re-renders ALL previous chunks. With hundreds of SSE events per request, this pegs the CPU at 100%. **Fix**: Only set `requests_dirty = true` on incoming events. Defer the actual rebuild to just before `terminal.draw()`, and only when `active_tab == 2` (Requests tab is visible). When the user is on the Prompt tab, zero rendering work happens for request data. Tested by `test_rebuild_requests_lines_is_lazy_not_eager`.

19. **`tokio::spawn` silently swallows panics + `unsafe` lifetime UB**: The conversation turn was spawned via `tokio::spawn` with the `JoinHandle` immediately dropped. If the task panicked, the panic message went to stderr (invisible under alternate screen), and the event loop never learned the task died — the UI hung forever with a spinner. Additionally, `api_client` was passed via a raw pointer cast to `&'static dyn ApiClient` (`unsafe`), which is Undefined Behavior if the event loop exits before the spawned task. **Fix**: (1) Changed `api_client` from `Box<dyn ApiClient>` to `Arc<dyn ApiClient>` — eliminates the `unsafe` block entirely. (2) Store the `JoinHandle` and poll `is_finished()` each loop iteration; on panic, extract the panic message, log it via `tracing::error!`, and display it in the UI. (3) Always send `TurnComplete` after the task finishes (even after `Error`), consistent with the `/compact` handler. (4) Added `tracing::error!` before sending `ConversationEvent::Error` so conversation failures appear in the debug log. Tested by `test_api_client_is_arc_not_box`, `test_run_tui_is_arc_not_box`.

20. **macOS Terminal sends Alt+b/Alt+f instead of Alt+Left/Alt+Right**: macOS Terminal (and many others with default settings) sends `ESC b` / `ESC f` for Option+Left / Option+Right. crossterm decodes these as `KeyCode::Char('b')` / `KeyCode::Char('f')` with `KeyModifiers::ALT`. Without explicit handling, the general `Char(c)` arm inserts a literal 'b' or 'f' into the input. **Fix**: Added dedicated match arms for `Char('b')` + ALT → `move_cursor_word_left()`, `Char('f')` + ALT → `move_cursor_word_right()`, and `Char('d')` + ALT → `delete_word_forward()` (Emacs binding), all placed before the general `Char(c)` arm. Tested by `test_event_loop_has_alt_b_word_left_binding`, `test_event_loop_has_alt_f_word_right_binding`, `test_event_loop_has_alt_d_delete_word_forward_binding`, `test_alt_b_does_not_insert_b`, `test_alt_f_does_not_insert_f`.

21. **Input area must soft-wrap long lines**: The old input height was based on `input.lines().count()` which only counts explicit `\n` newlines. Long text without newlines rendered as a single visual line that overflowed past the right border, making the cursor invisible and text unreadable. **Fix**: `input_visual_line_count(width)` computes visual line count by iterating chars and their `UnicodeWidthChar::width()`, wrapping at the given terminal column width. `input_cursor_visual_pos(width)` computes the (row, col) position of the cursor accounting for soft-wrap. `render_input()` uses `Paragraph::wrap(Wrap { trim: false })` so ratatui wraps the text, and the input area height grows dynamically (capped at 10 visual lines). **Never use `lines().count()` for input height** — it misses soft-wrap lines. Tested by `test_input_visual_lines_long_text_wraps`, `test_input_cursor_visual_pos_wrapped`, `test_input_cursor_visual_pos_at_wrap_boundary`, `test_input_cursor_visual_pos_cjk`, `test_input_visual_lines_capped_at_max`.

22. **Cursor position must match ratatui's word-wrapping**: `Paragraph::wrap(Wrap { trim: false })` uses **word-level wrapping** — when a word doesn't fit on the current line, the entire word moves to the next line. The old `input_cursor_visual_pos` used character-level wrapping (break at any char), causing cursor misalignment when a long word gets bumped to the next line. **Fix**: Rewrote `input_cursor_visual_pos` and `input_visual_line_count` with a two-pass algorithm that faithfully replicates ratatui's `WordWrapper` (trim=false): Phase 1 builds wrapped lines using the same state machine (pending_line/pending_ws/pending_word, word boundary flush, untrimmed overflow for char-breaking long words, whitespace draining at line breaks). Phase 2 looks up the cursor's char index in the wrapped lines. Consumed whitespace at line breaks (drained or skipped) maps to `(next_row, 0)`. **Key subtlety**: ratatui's `untrimmed_overflow` flushes the pending word to the line when `pending_line.is_empty() && word_w + ws_w + char_w > width`, enabling character-level breaking for words wider than the viewport. Without this, pure non-whitespace text (e.g. `"a".repeat(20)`) never triggers a line break. **CJK gotcha**: CJK characters without spaces form a single "word" — if the word overflows but the pending_word_overflow check (`line_w + ws_w + word_w >= width`) is false after the untrimmed flush, ratatui keeps them on the same line (content gets clipped, not wrapped). Tested by `test_input_cursor_visual_pos_word_wrap_last_word`, `test_input_cursor_visual_pos_word_wrap_mid_word`, `test_input_cursor_visual_pos_word_wrap_at_space`, `test_input_cursor_visual_pos_word_wrap_start_of_wrapped_word`, `test_input_visual_line_count_word_wrap`, `test_input_visual_line_count_word_longer_than_width`.

23. **macOS German keyboard: Fn+Option+Backspace sends `Char('(')` + ALT**: On German macOS keyboard layout, `Fn+Option+Backspace` (intended as "delete word forward") does NOT send `KeyCode::Delete` + ALT. Instead, macOS merges `Fn` (remaps Backspace→Delete) with `Option` (dead-key layer) and the terminal receives the character `(` (from `Option+8` on German layout) with ALT modifier. crossterm decodes this as `Char('(')` + `KeyModifiers::ALT`. Without explicit handling, the general `Char(c)` arm inserts a literal `(` into the input. **Fix**: Added a dedicated match arm for `Char('(')` + ALT → `delete_word_forward()`, placed before the general `Char(c)` arm. Normal `(` input (via `Shift+8`) arrives as `Char('(')` + SHIFT (no ALT), so it's not affected. **Workaround if other layouts have similar issues**: `Alt+d` (Emacs binding) always works for delete-word-forward. Tested by `test_event_loop_has_fn_option_backspace_german_binding`, `test_fn_option_backspace_does_not_insert_paren`.

24. **Shift+Enter and Alt+Enter are indistinguishable from Enter in most terminals**: Most terminals (macOS Terminal, iTerm2, GNOME Terminal) send the same escape code (CR / `0x0d`) for Enter, Shift+Enter, and swallow Alt+Enter entirely. Only terminals supporting the **Kitty keyboard protocol** (Kitty, WezTerm, foot, Ghostty) send distinct codes for modified Enter. **Fix**: (1) **Ctrl+J** (ASCII linefeed `0x0a`) is the primary newline shortcut — it works in **every** terminal because it's a completely different character code. (2) Shift+Enter and Alt+Enter are kept as additional bindings for Kitty-protocol terminals. (3) Kitty keyboard protocol enabled via `PushKeyboardEnhancementFlags(REPORT_EVENT_TYPES | DISAMBIGUATE_ESCAPE_CODES)` at startup — terminals that don't support it silently ignore the sequence. (4) `TerminalGuard::drop()` sends `PopKeyboardEnhancementFlags` to restore the terminal. Tested by `test_shift_enter_inserts_newline`, `test_shift_enter_inserts_newline_mid_text`, `test_shift_enter_multiple_newlines`, `test_event_loop_has_shift_enter_newline_binding`, `test_event_loop_has_alt_enter_newline_binding`, `test_event_loop_has_ctrl_j_newline_binding`, `test_ctrl_j_does_not_insert_j`.

25. **Up/Down arrows must move cursor vertically in multiline input**: When the input contains `\n` newlines or is soft-wrapped across multiple visual lines, Up/Down should move the cursor one visual line up/down (preserving display column where possible, clamping to end-of-line if the target line is shorter). **Only when the cursor is already on the first/last visual line** should Up/Down fall through to prompt history browsing (Up) or tab-bar navigation (Down). If the user is already browsing history (`history_index.is_some()`), Up/Down continue navigating history without attempting vertical cursor movement. **Implementation**: `move_cursor_up(width)` / `move_cursor_down(width)` return `bool` (true = moved, false = first/last line, caller should fall through). Uses `char_index_at_visual_pos()` — the inverse of `word_wrap_cursor_pos()` — to find the char index at a given visual (row, col). The word-wrap algorithm is duplicated from `word_wrap_line_with_cursor` to build wrapped lines and scan for the target position. Tested by `test_cursor_up_simple_newline`, `test_cursor_down_simple_newline`, `test_cursor_up_soft_wrap`, `test_cursor_down_soft_wrap`, `test_cursor_up_clamps_to_shorter_line`, `test_cursor_down_clamps_to_shorter_line`, `test_cursor_up_cjk_display_width`, `test_cursor_up_down_three_lines`, `test_event_loop_has_cursor_up_for_multiline`, `test_event_loop_has_cursor_down_for_multiline`.

26. **Bracketed paste: multiline paste must not trigger submit**: Without bracketed paste mode, pasting multiline text via Cmd+V sends each character individually, and `\n` is treated as Enter (submit). This truncates the paste at the first newline. **Fix**: Enable `EnableBracketedPaste` at terminal startup. The terminal wraps pasted text in escape sequences (`\e[200~` ... `\e[201~`), and crossterm delivers the entire paste as a single `Event::Paste(String)`. The `insert_paste()` method inserts the full text at the cursor position, preserving `\n` as literal newlines. Windows-style `\r\n` is normalized to `\n`. `DisableBracketedPaste` is sent on cleanup (TerminalGuard, panic hook, signal handlers). Tested by `test_insert_paste_multiline`, `test_insert_paste_at_cursor`, `test_insert_paste_empty`, `test_insert_paste_single_line`, `test_insert_paste_trailing_newline`, `test_insert_paste_utf8`, `test_insert_paste_crlf_normalized`, `test_insert_paste_into_multiline_input`, `test_event_loop_has_paste_handler`, `test_event_loop_enables_bracketed_paste`, `test_event_loop_disables_bracketed_paste`.

27. **UserQuestion tool: LLM asks the user questions via a TUI dialog**: The model can call `UserQuestion` with a `question` string and optional `options` array. The tool sends a `UserQuestionRequest` through an `mpsc::UnboundedSender` to the TUI, which displays a modal dialog. The user can navigate options with ↑↓, switch to free-text input with Tab, quick-select with number keys 1-9, submit with Enter, or cancel with Esc. Architecture mirrors the `PermissionPrompter` pattern (oneshot channel for the response). `has_modal()` helper on `App` returns `true` when either `pending_permission` or `pending_user_question` is active — used as a guard to suppress normal input handling during any modal dialog. The tool is injected into `ConversationState.tools` at TUI startup, not in `builtin_tools()`, because it needs the channel sender. Tested by `test_definition_name_is_user_question`, `test_call_sends_question_and_returns_answer`, `test_call_with_no_options`, `test_call_freetext_answer_with_options`, `test_call_fails_when_channel_dropped`, `test_call_with_utf8_question_and_answer`, `test_has_modal_true_with_user_question`, `test_user_question_nav_down_through_options`, `test_user_question_tab_switches_to_text`, `test_user_question_submit_option`, `test_user_question_text_input_utf8`, `test_event_loop_has_user_question_tool`, `test_event_loop_blocks_input_during_user_question`, `test_render_has_user_question_dialog`.

28. **`truncate_with_notice()` UTF-8 byte-slicing panic**: `String::truncate(max)` panics when byte `max` lands inside a multi-byte UTF-8 character. This is the same class of bug as Gotcha #16 (`&str[..N]` panics at non-char-boundary). The production crash was triggered by `WebFetch` fetching an Apple Developer page — `html2text` output contained typographic quotes, em-dashes, etc. `truncate(30000)` landed inside a multi-byte char → `assertion failed: self.is_char_boundary(new_len)`. **Fix**: Walk backwards from `max` with `is_char_boundary()` to find the last valid boundary before truncating. **Rule**: Never call `String::truncate(N)` or `&str[..N]` with a byte offset without first checking `is_char_boundary()`. Tested by `test_truncate_with_notice_utf8_boundary`, `test_truncate_with_notice_emoji_boundary`, `test_truncate_with_notice_exact_boundary`.

29. **Adaptive typewriter effect: measure SSE interval, distribute chars evenly**: SSE `TextDelta` events arrive every ~10-50ms and contain multiple characters (tokens). Simply dumping the whole token into the buffer at once looks "bursty" — text appears in clumps. **Fix**: An adaptive typewriter queue (`VecDeque<char>`) buffers incoming chars. On each new delta, the interval since the previous delta is measured, and `char_interval = sse_interval / new_char_count` is computed (clamped to [0.5ms, 50ms]). The event loop drains one char at a time from the queue into `streaming_buffer` at this adaptive rate. Poll timeout drops to ~2ms while the queue is non-empty. `TextComplete` flushes the remaining queue immediately, then pushes the final `DisplayBlock`. **Key**: The interval is recalculated on every delta — if the API speeds up or slows down, the typewriter effect adapts. The minimum clamp prevents invisible-fast rendering; the maximum prevents painful slowness on network stalls. Tested by `test_enqueue_delta_pushes_chars_to_queue`, `test_enqueue_delta_calculates_char_interval`, `test_drain_typewriter_respects_timing`, `test_drain_typewriter_utf8_char_by_char`, `test_flush_typewriter_empties_queue_to_buffer`, `test_interval_minimum_clamp`, `test_interval_maximum_clamp`, `test_interval_adapts_to_sse_cadence`, `test_finalize_text_complete_flushes_then_creates_block`, `test_event_loop_has_drain_typewriter`, `test_event_loop_poll_short_during_typewriter`.

30. **Markdown table columns overflow viewport**: `render_table()` computed column widths from cell content without any viewport width limit. Tables with long cell text produced `RenderedLine`s wider than the terminal. The generic `wrap()` then broke lines mid-border, destroying the visual table structure (misaligned `│` separators, torn `─` borders). **Fix**: `render_markdown_with_width(text, viewport_width)` passes the viewport width into `render_table()`. Column widths are shrunk proportionally to fit `viewport_width`. Long cell contents are word-wrapped (with char-level fallback for words wider than the column) into multi-line rows — every visual row gets its own `│`-bordered `RenderedLine`. **Fair minimum column width**: Pure proportional shrinking gives almost no space to narrow columns when another column has vastly more text (e.g. a 3-char "Key" column vs. a 120-char "Description"). `shrink_columns()` enforces a minimum of `available / (col_count + 1)` per column — for 2 columns each gets at least ~1/3 of the space. This prevents the first column from collapsing to 3 chars. The minimum only applies when shrinking is needed; naturally-fitting tables are unaffected. `rebuild_lines()` in `app.rs` calls `render_markdown_with_width(text, w)` instead of `render_markdown(text)` for `AssistantText` and streaming buffer, so tables always fit the terminal. The old `render_markdown(text)` delegates to `render_markdown_with_width(text, usize::MAX)` for backward compatibility. Tested by `test_md_table_wraps_long_cell_within_viewport`, `test_md_table_all_lines_same_width`, `test_md_table_wrapped_row_preserves_borders`, `test_md_table_narrow_viewport_still_renders`, `test_md_table_without_width_uses_natural_width`, `test_md_table_utf8_cell_wrap`, `test_md_table_visual_output`, `test_md_table_min_column_width_one_third`, `test_md_table_min_column_width_does_not_apply_when_natural_fits`.

31. **DuckDuckGo CAPTCHA blocks WebSearch**: DuckDuckGo's HTML endpoint (`html.duckduckgo.com/html/`) returns CAPTCHA pages ("Select all squares containing a duck") based on TLS fingerprinting and IP rate-limiting. The `reqwest` + `rustls` TLS fingerprint happened to pass DDG's bot detection, but `curl` (OpenSSL), Python (`urllib`), and other clients get blocked from the same IP. This is fragile — DDG could block rustls at any time. **Fix**: Switched to **Startpage** (Google proxy) as the primary search backend, with DuckDuckGo as fallback. Startpage serves Google results without CAPTCHAs. The parser strips inline `<style>` blocks (Emotion CSS-in-JS), then extracts results via `data-testid="gl-title-link"` markers with `<h2>` titles and `<p class="description">` snippets. CAPTCHA detection: `is_ddg_captcha()` checks for `"anomaly"` / challenge text; `is_startpage_blocked()` checks for absence of `gl-title-link`. `format_results()` now includes `[Source: Startpage]` or `[Source: DuckDuckGo]` in the output. If both backends fail, returns an `is_error: true` ToolOutput with a clear message. Tested by `test_parse_startpage_results_synthetic`, `test_parse_startpage_results_with_inline_styles`, `test_parse_startpage_results_with_bold_in_snippet`, `test_parse_startpage_results_with_html_entities`, `test_parse_startpage_results_respects_max`, `test_parse_startpage_results_utf8`, `test_parse_startpage_results_missing_snippet`, `test_is_startpage_blocked_*`, `test_is_ddg_captcha_*`, `test_startpage_parser_on_live_html`, `test_format_results_includes_source`.

32. **GTK emoji `is_emoji_char()` table incomplete — breaks ZWJ sequences and VS16 emoji**: The hand-curated `is_emoji_char()` table in `chlodwig-gtk/src/emoji.rs` only included `Emoji_Presentation=Yes` characters (emoji by default), but missed ~50 `Extended_Pictographic` characters that become emoji with VS16 (U+FE0F) or appear as components of ZWJ sequences. **Three concrete bugs**: (1) ☀ (U+2600), ⛈ (U+26C8), and ~48 other BMP chars were not detected as emoji → rendered as plain text via Pango instead of CoreText bitmap → invisible in PangoCairo (no color font support). (2) ♂ (U+2642) and ♀ (U+2640) were missing → ZWJ sequences like 🏊‍♂️ (swimmer + ZWJ + ♂ + VS16) got split at ♂: `Emoji("🏊\u200D")` + `Plain("♂\uFE0F")` → incomplete CoreText bitmap + orphan Pango glyph → wrong width (3 cols instead of 2) → table borders misaligned. (3) ⛈️ in weather table cells produced `Plain("⛈")` + `Emoji("\uFE0F")` → broken rendering. **Fix**: Expanded `is_emoji_char()` ranges to include all Unicode 16.0 Extended_Pictographic BMP characters: weather (U+2600..U+2604), gender signs (U+2640, U+2642), card suits (U+2660..U+2666), tools/objects (U+2692..U+269C), misc symbols (U+26A0, U+26A7, U+26B0..U+26B1, U+26C8, U+26CF, U+26D1, U+26D3, U+26E9, U+26F0..U+26F9). Existing ranges were consolidated (e.g. `U+26F2..U+26F3` + `U+26F5` → `U+26F0..U+26F5`). Tested by `test_is_emoji_char_sun`, `test_is_emoji_char_thunder_cloud`, `test_is_emoji_char_male_female_signs`, `test_is_emoji_char_misc_symbols_extended`, `test_is_emoji_char_symbols_and_religion`, `test_is_emoji_char_card_suits`, `test_is_emoji_char_misc_objects`, `test_is_emoji_char_misc_objects_2`, `test_split_zwj_swimmer_male_not_broken`, `test_split_zwj_cyclist_female_not_broken`, `test_split_multiple_zwj_emojis_in_table_cell`, `test_split_sun_with_vs16`, `test_split_thunder_cloud_with_vs16`, `test_split_weather_emojis_from_table`, `test_split_table_cell_sun_padded`.

33. **`ctx:` showed cumulative token sum instead of actual context window**: `format_status_left()` computed `ctx:` as `total_input_tokens + total_output_tokens` — the sum of ALL Usage events across ALL turns. This grows monotonically and has no relation to the actual context window size. The API reports `input_tokens`, `output_tokens`, and `cache_tokens` in each Usage event. The **last** Usage event's values ARE the current context window. **Fix**: Changed `ctx:` to use `TurnUsage::context_window_size()` (`input_tokens + output_tokens + cache_tokens`). Replaced the three separate `turn_input_tokens`/`turn_output_tokens`/`turn_cache_tokens` fields in `StatusLineData` with a single `turn_usage: &TurnUsage` reference. The context calculation now lives in one place: `TurnUsage::context_window_size()` in `chlodwig-core`. Both `App::context_window_size()` (TUI) and `AppState::context_window_size()` (GTK) delegate to it. The auto-compact threshold check in `event_loop.rs` also uses `app.context_window_size()`. The `total_*` fields remain in `StatusLineData` for the cumulative `tx:`/`rx:` display on the left status bar. Tested by `test_ctx_shows_last_turn_context_not_cumulative_total`, `test_ctx_cost_indicator_uses_turn_context`.

34. **GTK auto-scroll unified with TUI via `chlodwig_core::AutoScroll`**: The GTK version unconditionally scrolled to bottom on every new content event, making it impossible to read earlier output while streaming. The TUI had proper auto-scroll logic (disable when user scrolls up, re-enable at bottom) but it was duplicated inline. **Fix**: Extracted `AutoScroll` struct into `chlodwig-core/src/auto_scroll.rs` — a simple state machine with `is_active()`, `scroll_to_bottom()` (force on, for explicit user actions), `scroll_to_bottom_if_auto()` (no-op when user scrolled away, for incoming content), `user_scrolled_away()`, and `user_reached_bottom()`. Both TUI `App` and GTK `AppState` embed an `AutoScroll` field. TUI's `scroll_to_bottom()`/`scroll_to_bottom_if_auto()`/`scroll_up()`/`scroll_down()` delegate to it. GTK uses `vadjustment.connect_value_changed` to detect user scroll position: `at_bottom = (value + page_size) >= upper - 1.0`. The 1px tolerance handles floating-point rounding. The event loop calls `scroll_to_bottom_if_auto()` for incoming events and only performs the actual GTK scroll if `is_active()`. Explicit user actions (submit prompt) call `scroll_to_bottom()` to force re-enable. Tested by `test_auto_scroll_default_is_active`, `test_scroll_to_bottom_forces_active`, `test_scroll_to_bottom_if_auto_noop_when_inactive`, `test_scroll_to_bottom_if_auto_stays_active_when_active`, `test_user_scrolled_away_disables`, `test_user_reached_bottom_re_enables`, `test_round_trip_scroll_away_and_back`, `test_new_state_auto_scroll_is_active`, `test_clear_resets_auto_scroll_to_active`, `test_auto_scroll_user_scrolls_up_then_back`.

35. **GTK4: Never stack wrapped Labels in a ScrolledWindow — use TextView**: The Sessions browser window initially rendered each conversation message as a separate horizontal `GtkBox` containing a role `Label` and a body `Label` with `wrap(true) + hexpand(true)`, with `Separator`s between rows, all stacked in a vertical `GtkBox` inside a `ScrolledWindow` with a fixed height. Symptom: **the last ~100-250 pixels of content were unreachable via scroll** — the scrollbar's `upper` stopped short of the actual content end. **Root cause**: GTK4 uses height-for-width layout negotiation. For scroll extent calculation, `ScrolledWindow` sums the natural heights of its children. Wrapped `Label`s in horizontal boxes with `hexpand=true` consistently **under-report** their height — they compute height against a "preferred" width that differs from the final allocated width after `hexpand` negotiation with siblings. The error is small per widget (2-5 px) but accumulates across dozens of messages and separators. **Fix**: Use a single `TextView` with a `TextBuffer` as the message display. `TextView` performs wrap and height calculation **internally** via Pango — the same pipeline used for actual glyph rendering, so the measurement is exact. Role labels ("You"/"Assistant") are styled via `TextTag`s (weight=700, foreground color) inserted into the buffer, not separate widgets. **Rule**: For any scrollable block of text content with multiple logical messages/entries → use **one `TextView` + `TextBuffer` + `TextTag`s**, not a column of `Label`s. `ScrolledWindow + TextView` is GTK's canonical pattern for this exact use case (it's how the main output view works). Only use `Label` for short, fixed-size text where wrapping height is not a sizing input to a scrollable parent.

36. **GTK4 nested ScrolledWindows: scroll events leak to parent, cannot be stopped via propagation alone**: When you put a `ScrolledWindow` inside another `ScrolledWindow` (e.g. expandable message preview inside a session list), scroll events over the inner widget ALWAYS propagate to the outer — both scroll simultaneously. The intuitive fixes don't work: (1) A `Bubble`-phase controller on the inner returning `Propagation::Stop` runs AFTER the inner's Target-phase internal handler, but the event still propagates up to the outer's internal handler. (2) A `Capture`-phase controller on the outer that returns `Stop` blocks the outer AND the inner (events never reach the inner). (3) Adding `EventControllerScroll::VERTICAL` with `Stop` on the inner in any phase does not override the `ScrolledWindow`'s built-in scroll handler — that handler is bound internally and still fires. **The only reliable fix is to drive the inner adjustment manually and block all internal handlers**: (a) `Rc<RefCell<Option<ScrolledWindow>>> active_inner` tracks which inner area the cursor is over via `EventControllerMotion::connect_enter/leave` on each inner `ScrolledWindow`. (b) `Capture`-phase controller on the OUTER `ScrolledWindow`: if `active_inner.is_some()`, manually apply the delta via `inner.vadjustment().set_value(val + dy * step)` and return `Propagation::Stop` — the outer's internal handler never sees the event. If `None`, return `Proceed` so the outer scrolls normally. (c) `Capture`-phase controller on EACH inner `ScrolledWindow` that always returns `Stop` — this blocks the inner's own built-in scroll handler, which would otherwise run in addition to our manual `set_value()`, causing double-speed scrolling. (d) The `step` multiplier matters: macOS trackpad sends many small `dy` deltas (~1.0 each) per gesture. `step=50` → feels 10× too fast; `step=5` feels natural. Implemented in `chlodwig-gtk/src/sessions_window.rs`.

37. **GTK Markdown render must have a SINGLE entry point — never split "with code highlighting" from "with table-header tags"**: There used to be two functions rendering `&[StyledLine]` into a `gtk4::TextBuffer`: `md_renderer::append_styled_lines()` handled fenced-code syntax highlighting via syntect, and `render::append_styled_lines_with_table_headers()` handled clickable table-header sort tags. The two were copy-paste descendants — the table-aware variant was added later for the click-to-sort feature and forgot the `should_highlight_code_line()` branch. Symptom: a `\`\`\`rust` block in an AssistantText response streamed in colored, then on `TextComplete` the buffer was wiped and re-rendered through the table-aware function → uncolored monospace output. The two concerns are **orthogonal**: a code-body line never starts with `┌`/`├`/`└` and a header row never has `code_info=Some(_)`. **Fix**: collapse to ONE function `md_renderer::append_styled_lines(buf, lines, all_tables, block_idx)` that handles three priority-ordered cases per line: (1) code block → syntect, (2) table header → emit `table_sort:N:M` tags, (3) plain row. The table-header detection is extracted into a pure function `detect_table_header_rows(&[StyledLine]) -> Vec<Option<usize>>` so it can be unit-tested without a GTK display server. **Rule**: any new GTK render variant must extend the existing function via parameters, never a parallel copy. Tested by `test_no_separate_with_table_headers_function`, `test_unified_render_lives_in_md_renderer`, `test_unified_render_handles_table_headers`, `test_unified_render_handles_code_highlighting`, `test_render_rs_no_duplicate_render_function`, `test_callers_use_unified_function`, `test_detect_table_header_rows_simple`, `test_detect_table_header_rows_two_tables`, `test_detect_table_header_rows_no_tables`, `test_detect_table_header_only_first_row_after_top_border`.

38. **Cooperative turn-loop interrupt (`/stop`, `stop`, double-Esc)**: Users sometimes want to stop a runaway agentic turn loop (e.g. model keeps calling tools). A hard abort would leave the conversation in an invalid state — the Anthropic API **requires** that every `tool_use` block have a matching `tool_result` in the next user message, else the next API call 400s. **Fix**: `ConversationState.stop_requested: Arc<AtomicBool>`. `run_turn` resets it to `false` on entry, then checks it after each SSE `message_stop` AND **before every individual sequential tool execution** AND before each concurrent batch. If set **and** there are pending or unfinished tool_uses, it: (a) synthesizes a cancelled `tool_result` (`is_error: true`, text `"Tool execution cancelled by user."`) for each remaining tool_use — satisfying the API's pairing requirement, (b) appends a user-role Text block with the constant `INTERRUPT_MESSAGE` = `"User intentionally interrupted the turn loop."`, (c) clears the flag and recurses once (`Box::pin(run_turn(...))`) for a final assistant acknowledgement, then returns. **Mid-batch cancellation**: when N sequential tool_uses are pending and tool 1 has finished, pressing Esc-Esc (or `/stop`) before tool 2 starts → tools 2..N never call `tool.call()`; their `ContentBlock::ToolResult`s are synthetic cancellations. The user message in the next API round contains 1 real result + (N-1) synthetic results — preserving 1:1 pairing. **Already-running tools are NOT killed** (no SIGTERM, no `child.kill()`); only the *boundary between tool executions* is the cancellation point. Concurrent batches are atomic — they either all run or all get cancelled (no mid-batch abort). UI triggers: `/stop` command, bare `stop` (case-insensitive), and double-Escape within 500ms while loading. All three paths just `stop_flag.store(true, SeqCst)` on a shared `Arc<AtomicBool>` cloned from `state.stop_requested` at startup (so the flag stays in sync with the background task without needing to acquire the Mutex). **Never add a mid-stream abort via `cancel_safe` drop of the SseStream** — that leaves the assistant message half-parsed and the tool_use/tool_result pairing broken. Always let the current SSE round complete, then synthesize the cancellation. Tested by `test_stop_requested_cancels_pending_tools_and_injects_interrupt_message`, `test_stop_flag_reset_at_start_of_run_turn`, `test_stop_without_pending_tools_is_noop`, `test_stop_preserves_tool_use_tool_result_pairing`, `test_stop_during_first_sequential_tool_skips_remaining_tools`, `test_stop_after_all_tools_completed_still_injects_interrupt_for_next_round` (core); `test_event_loop_handles_command_stop`, `test_event_loop_has_double_esc_stop_trigger`, `test_event_loop_stop_flag_is_shared_with_conversation_state` (TUI); `test_submit_handles_command_stop`, `test_submit_context_has_stop_flag_field`, `test_submit_wires_double_esc`, `test_main_rs_shares_stop_flag_with_background` (GTK).

39. **GTK Edit-tool render must have a SINGLE helper — never copy-paste the diff rendering across the three render paths**: There are THREE render paths in `crates/chlodwig-gtk/src/render.rs` that all render `DisplayBlock::ToolUseStart`/`ConversationEvent::ToolUseStart`: (a) `render_event_to_buffer()` — live SSE streaming, (b) `render_restored_blocks()` — append render after `--resume` / `/resume`, (c) `rerender_all_blocks()` — full re-render after table interaction. Each one had its own copy of "how to render an Edit tool_use", and they had **drifted**: only `render_event_to_buffer()` used `render_highlighted_line()` with syntax highlighting. `render_restored_blocks()` had inline `format!("- {line}\n")` / `format!("+ {line}\n")` with `diff_remove`/`diff_add` tags but NO syntax highlighting → restored Edit diffs appeared as raw monochrome text. `rerender_all_blocks()` didn't even special-case `Edit` at all → after a table interaction the colored Edit diff was replaced by `── Tool: Edit ──` + raw JSON dump. **Fix**: extract `render::render_edit_tool_use(buffer, input)` as the SINGLE source of truth — uses `render_highlighted_line()` for both `old_string` and `new_string`. All three render paths call this helper. **Rule**: same as Gotcha #37 — every tool-specific render branch in GTK must live in exactly one function; render paths only choose which event/block to dispatch on, never how to render the tool. Tested by `test_render_edit_tool_use_helper_exists`, `test_render_event_to_buffer_uses_edit_helper`, `test_render_restored_blocks_uses_edit_helper`, `test_rerender_all_blocks_uses_edit_helper`, `test_render_restored_blocks_does_not_inline_diff_for_edit`. **Update (Gotcha #40)**: the three render paths themselves were collapsed into ONE — see Gotcha #40.

40. **GTK Variant B: Final + Streaming TextView, single render path**: The previous architecture had THREE rendering functions (`render_event_to_buffer` for live events, `render_restored_blocks` for session restore, `rerender_all_blocks` for resize/toggle), each with its own copy of tool-rendering logic that kept drifting (see Gotcha #39). Additionally, the streaming text was rendered into the SAME `TextBuffer` as the history, requiring an `Rc<Cell<i32>>` `streaming_start_offset` to track where to delete-and-rewrite on each delta — fragile and flicker-prone. **Fix (Variant B)**: (a) **Two TextViews** stacked vertically inside ONE `ScrolledWindow`: `final_view` (append-only history) and `streaming_view` (hidden when empty). (b) **One `render_block(buffer, &DisplayBlock, &RenderCtx)` function** is the single source of truth for block rendering. (c) `append_block` is a thin wrapper for the live event-loop append path. (d) `render_all_blocks_into(buffer, state, viewport_width, include_cwd_header)` is the SINGLE function for resize/restore/toggle (the only difference between rerender and restore was the CWD header — now a bool flag). (e) `render_streaming_into(buffer, text, viewport_width)` renders the live streaming buffer into the SEPARATE streaming view's buffer; returns `bool` for visibility toggle. (f) `render_event_to_buffer` is **deleted** — events flow through `state.handle_event()` which produces a `DisplayBlock`, then `append_block` renders it. (g) `streaming_start_offset` is **deleted** entirely. (h) `DisplayBlock::ToolResult` gains `tool_name: String` + `tool_input: serde_json::Value` so the renderer can produce rich Bash-ANSI / Read-line-numbers / Write-summary output even on session restore — `restore_messages` populates these fields from `RestoredBlock::{BashOutput, ReadOutput, WriteOutput, GrepOutput, ToolResult}`. (i) The `tool_id_to_info: HashMap<String, (name, input)>` previously living in `event_dispatch.rs` moved into `AppState` so `handle_event` can populate `tool_name`/`tool_input` on the `ToolResult` block at event time. **Result**: ONE block-render function, ONE full-rerender function, ONE streaming function. No drift possible — adding a new block type or a new tool-specific renderer touches exactly one place. **Rule for any new render path**: extend `render_block` (for the dispatch on block kind) or extend `render_tool_result` (for new tool-specific rendering); never add a parallel render function. Tested by `test_ui_widgets_has_final_and_streaming_views`, `test_window_builds_two_text_views_in_vertical_box`, `test_streaming_start_offset_is_gone`, `test_render_event_to_buffer_is_gone`, `test_render_has_single_block_renderer`, `test_render_has_append_block_for_live_events`, `test_render_has_render_all_blocks_for_resize_and_restore`, `test_render_restored_blocks_is_gone`, `test_rerender_all_blocks_is_gone`, `test_event_dispatch_uses_append_block_for_live_events`, `test_event_dispatch_renders_streaming_into_streaming_view`, `test_display_block_tool_result_has_tool_metadata`, `test_session_compatibility_serde_default_on_tool_metadata`.
43. **GTK `append_*` family must collapse to ONE implementation**: `chlodwig-gtk/src/window.rs` exposes three functions for adding text to an `EmojiTextView`: `append_to_output(view, text)` (no tag), `append_styled(view, text, tag)` (one tag), `append_multi_styled(view, text, &[tags])` (N tags). They are three specializations of the same operation — segment text via `emoji::split_emoji_segments`, insert plain segments with `buffer.insert()` + optional `apply_tag_by_name()`, route emoji segments through `insert_emoji_as_overlay()`. Originally each had its own copy of the segment loop. **Risk**: same drift class as Gotchas #37 / #39 / #40 — a bug fixed in `append_styled` could be missed in `append_multi_styled` and vice versa. **Fix**: introduce a single private/public `pub fn append(view: &EmojiTextView, text: &str, tag_names: &[&str])` that handles the union case (`tag_names.is_empty()` → no `apply_tag_by_name` call). The three named functions become `#[inline]` one-line wrappers (`append(view, text, &[])`, `append(view, text, &[tag_name])`, `append(view, text, tag_names)`). Wrappers are kept because Rust has **no function overloading** — the named call-sites (`append_styled(v, msg, "error")`) read better than `append(v, msg, &["error"])`, and migrating ~50 call-sites would be a huge churn diff. The wrapper layer costs zero (inlined) and gives both single-source-of-truth (impossible to drift) AND call-site readability. **Rule**: any future variant (e.g. `append_with_link(...)`) must be added as another wrapper that delegates to a single, possibly-extended core `append()` — never as a parallel reimplementation of the segment loop. Tested by `test_window_has_unified_append_function`, `test_append_to_output_delegates_to_append`, `test_append_styled_delegates_to_append`, `test_append_multi_styled_delegates_to_append`, `test_only_unified_append_contains_emoji_split_logic` (asserts `split_emoji_segments(text)` appears exactly once in `window.rs`).

44. **GTK subclass + `thread_local!` global registry = cross-instance SIGSEGV**: `EmojiTextView` originally stored its overlay entries in a `thread_local! static EMOJI_ENTRIES: RefCell<Vec<EmojiEntry>>`. With ONE view this works; with TWO views (`final_view` + `streaming_view`, see Gotcha #40) it crashes. Each `EmojiEntry` holds a `gtk4::TextMark` that is bound to a specific `TextBuffer`. In `snapshot()`, both views iterate the SAME global `EMOJI_ENTRIES` and call `widget.iter_at_mark(&entry.mark)` / `widget.iter_location(&iter)` against THEIR OWN buffer — but for half the entries the mark belongs to the OTHER view's buffer. GTK accepts the foreign mark, derives a garbage `GtkTextLine*` from it, and `_gtk_text_line_get_number` dereferences it → `EXC_BAD_ACCESS (SIGSEGV) KERN_INVALID_ADDRESS … (possible pointer authentication failure)` on macOS arm64. The crash log (`crash-2026-04-19-20-26-22.0403.log`) showed the chain: `_gtk_text_line_get_number` → `gtk_text_line_display_compare` → `gtk_text_layout_get_iter_location` → `iter_location` (emoji_overlay.rs:192) → `EmojiTextView::snapshot`. **Fix**: move the registry onto the GObject subclass as a per-instance field: `pub struct imp::EmojiTextView { entries: RefCell<Vec<EmojiEntry>> }`. Drop ALL free functions (`insert_emoji(buffer, …)`, `clear_overlays(buffer)`, `clear_overlays_from(buffer, offset)`, `overlay_count()`) — they made it syntactically possible to write into one buffer's overlay set while reading from another. The public API becomes inherent methods on the wrapper: `view.insert_emoji(texture, scale, placeholder_len, tags)`, `view.clear_overlays_from(offset)`, `view.overlay_count()`, `view.clear()`. Cascade: every render path that previously took `&gtk4::TextBuffer` for emoji insertion is migrated to `&EmojiTextView` (md_renderer, render, restore, submit, menu, table_interactions, event_dispatch, main). **Defensive belt-and-suspenders**: even with a per-instance registry, the snapshot loop checks `if entry.mark.buffer().as_ref() != Some(&buffer) { continue; }` — guards against a future regression that re-introduces sharing. **Rule for any GTK4 subclass that holds buffer-bound state** (marks, iters, anchors, child widgets, paintables tied to a TextChildAnchor): the state MUST live as a `RefCell<…>` field on the `imp::` struct, NEVER in a `thread_local!` or `static` — otherwise multiple instances will trample each other's references the first time a sibling widget is added. Tested by `test_emoji_entries_is_not_thread_local_static` (greps `emoji_overlay.rs` for `static EMOJI_ENTRIES` and `thread_local!`, must be absent), `test_imp_emoji_text_view_has_entries_field` (parses the `mod imp { … }` block, asserts `entries: … Vec<EmojiEntry>` field), `test_emoji_text_view_has_inherent_insert_emoji_method`, plus 3 more source-grep guards in `tests/emoji_view_method_tests.rs`.

45. **GTK read-only TextView: `set_can_focus(false)` + `has_selection()` guard on the drag controller (issue #27)**: Two layers caused "click → snap to top" on the read-only output. **Layer 1**: `disable_text_view_drag()` called `buf.select_range(&iter_at_offset(cursor_position), ...)` unconditionally on every drag-begin. Cursor at offset 0 → moves insert mark to 0 → GTK auto-scrolls insert mark on-screen → snap to top. Fix: guard with `if buf.has_selection()`. **Layer 2 (the actual root cause)**: even after Layer 1, clicks still snapped because GTK4 `TextView`'s internal click handler places the cursor at the click position and calls `scroll_mark_onscreen(insert_mark)`. `set_focus_on_click(false)` was NOT enough — the click handler still runs. The robust fix is `set_can_focus(false)` on both `final_view` and `streaming_view`: the views can never receive focus, period. Click-and-drag selection still works (`GestureDrag` does not require focus); right-click "Copy" still works (popover dispatches the action without the view being focused). **Rule for any read-only `TextView` inside a `ScrolledWindow`**: `set_can_focus(false)` is the minimum baseline. `set_focus_on_click(false)` alone is insufficient. Tested by `test_disable_text_view_drag_guards_select_range_with_has_selection` (in `tests/click_no_scroll_top_tests.rs`) and `test_output_views_have_can_focus_false` (in `tests/scroll_simplification_tests.rs`).

46. **GTK auto-scroll-to-bottom: ONE closure, TWO calls per content-change tick (issue #27 final form)**: The journey to this fix went through three failed attempts. (a) `idle_add_local_once { scroll_to_bottom() }` raced with layout — read stale `upper`, landed inside the just-finalized block. (b) `notify::upper` + `notify::page-size` handlers — the two properties settle in separate layout passes (worsened by `streaming_view.set_visible(false)` in our two-`TextView` stack), so handlers fired with stale values; last line half-cropped, scroll-up-and-back left viewport off-bottom. (c) Per-tick pin only — works for streaming text but on `TextComplete` the content moves from `streaming_view` to `final_view`, GTK takes a frame to re-layout, so the per-tick pin reads stale values and the viewport jumps 16ms later (= the "ruckeln"). **Final form**: ONE closure (`if auto_scroll.is_active() { let new_bottom = upper - page_size; if new_bottom > 0 && |value - new_bottom| > 0.5 { set_value(new_bottom); } }`) called TWICE per content-changing tick: (1) immediately, end of tick — cheap, catches the common case of settled layout; (2) via `idle_add_local_once` (default idle priority 200, runs AFTER `GTK_PRIORITY_RESIZE` = −90), so the layout pass for this tick's mutations is guaranteed complete. The idle callback re-checks `auto_scroll.is_active()` in case the user scrolled away between tick and idle. The idle is only scheduled when `needs_scroll` is true (a new block was rendered, streaming buffer changed, or visibility was toggled this tick) — pure no-op ticks don't queue idle work. The `value_changed` handler in `main.rs` still tracks user-scroll position (read-only on the adjustment, no race) to flip `auto_scroll.is_active()` on/off. **Rule for any "tail-the-bottom" widget in GTK4**: do NOT chain `notify::*` handlers; do NOT rely solely on per-tick polling; the canonical "post-layout pin via default idle priority" runs after GTK's resize cycle and gives reliable values. Tested by `test_main_no_longer_uses_notify_upper_or_page_size_handlers`, `test_event_dispatch_pins_value_to_bottom_when_auto_scroll_active`, `test_event_dispatch_schedules_post_layout_pin_after_content_changes` (in `tests/scroll_simplification_tests.rs`).

45. **GTK `RefCell::borrow()` held across `adj.set_value()` → BorrowMutError → abort across FFI**: The auto-scroll block in `event_dispatch.rs` did `let state = state_for_events.borrow(); if state.auto_scroll.is_active() { adj.set_value(new_bottom); }`. `gtk4::Adjustment::set_value()` **synchronously** emits the `value-changed` signal, which dispatches into the user-scroll handler registered in `main.rs` (`adj.connect_value_changed(move |adj| { let mut state = state_for_scroll.borrow_mut(); … })`). Both closures capture clones of the SAME `Rc<RefCell<AppState>>`. The shared (immutable) borrow is still alive when the value-changed handler runs → `RefCell::borrow_mut()` panics with `already borrowed: BorrowError` → the panic crosses the GLib FFI boundary (signal trampoline) → `panic_cannot_unwind` → `std::process::abort()`. Crash log: `crash-2026-04-20-13-02-07.log` (Thread 0: `__pthread_kill` ← `abort` ← `panic_with_hook` ← `value_changed_trampoline` ← `g_signal_emit` ← `adjustment_set_value` ← `event_dispatch.rs:305`). **Fix**: hoist any data needed across the `set_value()` call into a local (`let auto_active = state.borrow().auto_scroll.is_active();`) so the borrow's temporary is dropped at the end of the statement, BEFORE `set_value()` runs. **Rule**: NEVER hold a `RefCell` borrow (immutable OR mutable) across any GTK call that can synchronously emit a signal — `set_value`, `set_text`, `set_active`, `set_visible`, `set_sensitive`, `add_css_class`, `queue_draw`, etc. all chain into user-registered handlers that may call `borrow_mut()` on the same cell. The safe pattern is: borrow → read primitives → drop borrow → call GTK setter. Tested via source-grep regression tests `test_no_borrow_held_across_set_value_in_auto_scroll_block` and `test_idle_scroll_callback_does_not_hold_borrow_across_set_value` in `event_dispatch.rs::tests` (GTK widgets cannot be instantiated in unit tests without a display server, so the guard is at the source level).

47. **GTK pure-focus scroll model: bottom spacer + `was_at_content_bottom` snapshot, NOT `auto_scroll` state**: Earlier attempts to "pin the viewport" used the `chlodwig_core::AutoScroll` state machine in `event_dispatch.rs` to decide between snapping to the bottom vs. freezing in place. This was unreliable because GtkAdjustment **automatically clamps `value` to `[0, upper - page_size]`** whenever `upper` shrinks — and `upper` shrinks every single time `streaming_view.set_visible(false)` runs (on `TextComplete`, finalize). The auto-clamp synchronously fires `value-changed` → the user-scroll handler in `main.rs` interprets it as "user scrolled away" → flips `auto_scroll` to inactive → next tick freezes at the clamped position even though the user was just following the stream. Symptom: viewport "kurz unten dann jumped es hoch" right after the stream finished. **Fix (two pieces)**: (1) **Bottom spacer**: a third child below `final_view` + `streaming_view` in `output_inner_box`, fixed `height_request(4000)` (no vexpand, no dynamic resize). Keeps `upper` so generously inflated that GtkAdjustment never clamps `value` while the stream finalizes. Cost: scrollbar daumen is too small and you can scroll a bit past the actual content into empty space. Acceptable trade-off. (2) **`was_at_content_bottom` snapshot**: at the start of every 16ms tick, BEFORE any mutations, capture `value_before = adj.value()` and compute `was_at_content_bottom = (value_before + page_size) >= (final_h + stream_h) - 1.0`. This single boolean — derived directly from raw adjustment values, NOT from `auto_scroll.is_active()` — is the source of truth for the entire tick. After all events are processed and the layout pass settles (via `glib::idle_add_local_once`, priority 200 > resize priority −90), apply ONE of two rules: `was_at_content_bottom == true → snap to NEW content_bottom - page_size` (follow); `was_at_content_bottom == false → restore value_before` (pin exactly). No heuristics, no per-event tracking, no `pin_to_content_bottom` combiner, no `user_was_in_streaming_before`, no `streaming_just_finalized`. **Rule**: when GTK's adjustment auto-clamp can corrupt your "is the user at the bottom?" classifier, do NOT layer more heuristics on top — instead inflate `upper` (spacer) so the clamp never fires, and read raw adjustment values once per tick before any mutation. Tested by `test_event_dispatch_uses_was_at_content_bottom_strategy`, `test_event_dispatch_does_not_dynamically_resize_spacer`, `test_event_dispatch_does_not_use_pin_to_content_bottom_combiner`, `test_event_dispatch_does_not_consult_auto_scroll_for_scroll_commands`, `test_window_bottom_spacer_has_fixed_height`, `test_main_uses_content_bottom_for_at_bottom_check` (in `tests/scroll_anchor_tests.rs`).

49. **GTK streaming→final re-render: pin `value_before`, never snap to content_bottom**: When a streaming text block migrates into `final_view` on `TextComplete` (or `TurnComplete` / `Error` / `ToolUseStart` / `CompactionStarted` while streaming_buffer is non-empty), the re-render can produce a slightly different height than the live stream (different word-wrap because the final view and streaming view have independent layout passes, different code-block padding, list spacing, etc.). If the tick-restore rule is `was_at_content_bottom → snap to content_bottom_now - page_size`, that snap actively moves the user UP by the height delta → "jumps up while following the stream" bug. **Fix**: in the tick-restore section of `event_dispatch.rs`, check `streaming_just_finalized` FIRST and, if set, pin `value_before` exactly (clamped to `[0, upper - page_size]`). Only when `streaming_just_finalized == false` does the `was_at_content_bottom` branch apply (for e.g. a new `ToolResult` block appended while stream was idle and user was at bottom). **Invariant**: on a streaming→final transition NOTHING visually moves at the user's pixel position in `output_inner_box` absolute coords — so the viewport offset that kept the user looking at pixel P before the transition keeps them looking at pixel P after. No measurement, no math, no race. The `streaming_just_finalized` flag is set in the event loop when processing one of the finalizing events while `state.streaming_buffer` was non-empty. Tested by `test_restore_pins_value_before_on_streaming_finalize` (source-grep guard in `tests/scroll_anchor_tests.rs`).

50. **GTK Stage C multi-window: `build_window` is the SSoT, drag-out empty placeholder, schema bump v1→v2**: Multi-window adds three failure modes that the single-window design glossed over. (a) **Per-window wiring drift**: copy-pasting the close handler / persistence / focus / title-follow / `connect_create_window` setup into "initial window", "Cmd+N action", and "drag-out callback" guarantees they will diverge — every bug fix needs to land in three places. **Fix**: `tab::build_window(ctx, Option<WindowState>, fallback_cwd) -> ApplicationWindow` is the ONLY function that creates a window. All three call sites delegate to it. New per-window wiring goes in there or nowhere. (b) **Drag-out placeholder race**: `connect_create_window` must return the new `TabView` so libadwaita can migrate the dragged page into it. If `build_window` always attaches a placeholder fresh tab, the new view ends up with TWO tabs after migration (placeholder + dragged). **Fix**: pass `Some(WindowState::empty())` for drag-out → the `Some(ws)` branch with `ws.tabs.is_empty()` deliberately attaches NOTHING, leaving the view empty for libadwaita. The `None` branch (used by Cmd+N and initial fresh window) attaches one fresh tab. Crucially: NEVER try to `close_page` a placeholder after the fact — `close_page_finish(true)` triggers the close handler which calls `window.close()` on `n_pages() == 0`, killing the brand-new window. (c) **Schema bump**: v1 stored a single window inline (`{version:1, tabs:[...], active_index:N}`); v2 wraps in a `windows` array (`{version:2, windows:[{tabs:[...], active_index:N}, ...]}`). **Fix**: `parse_window_state(json)` peeks at the JSON: top-level `windows` field → v2 direct parse; top-level `tabs` field → v1, wrap as one-element list; otherwise propagate serde error. Migration is read-only — next save writes v2. (d) **Per-window persistence saves the WHOLE app set**: a per-window `connect_page_attached` handler that snapshots only its own window would clobber the other windows on every tab change. **Fix**: `wire_tab_set_persistence(tab_view, app_registry)` takes the **app-level** `AppWindowRegistry` and calls `save_app_window_state(app_registry)` which iterates every window. (e) **Active-window dispatch in menu**: a single menubar serves all windows in libadwaita. Each menu action calls `active_entry(&app, &app_registry)` — looks up `app.active_window()` and finds the matching `WindowEntry` (which carries `tab_view`, `registry`, `config`, `window` together). `WindowEntry` MUST carry `config` and `window` fields directly; reaching into `app_registry.first()` to "borrow" config from another window is a red flag that the SSoT is wrong. (f) **Cmd+N reassigned**: was bound to `app.new-conversation`; Stage C moves it to `app.new-window`. Two actions on the same accelerator silently break one of them. Tested by `test_meta_n_bound_to_new_window_not_new_conversation`. Full test suite in `tests/multiwindow_stage_c_tests.rs` (18 source-grep guards) + `chlodwig_core::window_state` (18 unit tests covering v1/v2 round-trips, migration, clamping). **Rule**: any per-window wiring (handlers, registry membership, persistence, drag-out) goes in `build_window` and nowhere else.

48. **GTK4 `add_tick_callback` vs. `idle_add_local_once` for post-layout measurement**: `scroll_to_content_bottom()` is called by `submit.rs` immediately after appending the user message ("▶ Hello") to the `final_view` buffer. The naive implementation reads `final_view.allocated_height()` synchronously — at which point GTK has not yet run a layout pass, so the height returns the PRE-mutation value. Scroll target is short by exactly the height of the new user message; the user's prompt vanishes below the visible area. **First fix attempt**: wrap in `glib::idle_add_local_once`. This works for many GTK widgets but **NOT for GTK4's frame-clock-driven layout**: idle priority (`G_PRIORITY_DEFAULT_IDLE = 200`) is higher than `GTK_PRIORITY_RESIZE = -90`, but GTK4 layout runs on the **frame clock**, not the GLib main loop. Idle callbacks can fire BEFORE the next frame's layout pass completes → `allocated_height()` still stale → bug persists. **Correct fix**: `Widget::add_tick_callback(|widget, frame_clock| ControlFlow)` runs in sync with the frame clock, AFTER the layout pass for that frame. Schedule a 2-frame countdown: skip the first frame (which only invalidates the layout), measure on the second frame (which has the new size), return `ControlFlow::Break` to unregister. The 2-frame budget handles the case where the first frame after buffer mutation only marks the layout dirty without committing the new size. **Rule**: for any "measure widget size, then act" code in GTK4 that runs immediately after a buffer/widget mutation, use `add_tick_callback` with a frame countdown — not `idle_add_local_once`. **Caveat**: this only matters for ONE-SHOT measurements. The 16ms tick-restore loop in `event_dispatch.rs` still uses `idle_add_local_once` because it's self-correcting (next tick re-measures and converges within one frame), and Streaming→Final transitions usually have ~0 px height delta (same renderer) so any single-frame stale read is invisible. Tested by `test_scroll_to_content_bottom_uses_tick_callback` (source-grep guard in `tests/scroll_anchor_tests.rs`).

51. **macOS 26: GTK4 DnD aborts process via foreign exception (TWO root causes, not one)**: Starting with macOS 26 (Tahoe), AppKit raises `NSInvalidArgumentException` for cases that previous macOS versions silently tolerated. The exception unwinds through Rust frames → `__rust_foreign_exception` → `abort()` → no panic hook, no signal handler, just dead process. Stack pattern in crash log: `__pthread_kill` ← `abort` ← `_RNvCsXXX_7___rustc24___rust_foreign_exception` ← `lang_start_internal` ← `main`. **Two independent bugs trigger the same crash signature** — fixing only one keeps the crash. (a) **`-localObject` selector missing on `GdkMacosPasteboardItem`**: `NSCoreDragManager` calls `[item localObject]` during drag setup. AppKit treats the missing selector as fatal on macOS 26. **Fixed upstream** in GTK commit `ee75754336` ("macos: Fix Drag-n-drop for macOS 26", first in 4.21.4) — adds `-(id)localObject { return nil; }`. MacPorts ships only `gtk4 4.14.3` on the stable channel; switch to `gtk4-devel @4.22.2+quartz` to get the fix. (b) **`application/x-rootwindow-drop` registered as NSPasteboardType**: `gtk_window_init()` unconditionally creates a `GtkDropTargetAsync` for the GTK-internal MIME `application/x-rootwindow-drop` (used by `GtkNotebook` for tab-detach to the X11 root window). On macOS 26, AppKit raises `NSInvalidArgumentException("'application/x-rootwindow-drop' is not a valid UTI string")` the moment ANY drag-out begins. The 2017 GTK3 fix (commit `6a770517cc`, "Only register application/x-rootwindow-drop on X11") was never ported to GTK4. The crash is NOT in the drop-target setup — it's in `[GdkMacosPasteboardItemDataProvider types]`, which calls `_gdk_macos_pasteboard_to_ns_type()` for every MIME in the drag-source's `storable_formats`. For unknown MIMEs it falls through to `g_content_type_from_mime_type()`, which on Darwin pass-throughs the MIME unchanged → invalid NSString fed to `setDataProvider:forTypes:` → exception. **Two patches required**, both maintained as MacPorts-style local patches in `~/macports-patches/`: `patch-rootwindow-drop-x11only.diff` (wraps the `gtk_window_init` block in `#ifdef GDK_WINDOWING_X11 + GDK_IS_X11_DISPLAY()`) AND `patch-pasteboard-skip-rootwindow.diff` (adds an explicit `g_strcmp0(mime, "application/x-rootwindow-drop") == 0 → return nil` branch in `_gdk_macos_pasteboard_to_ns_type`). The second one is the load-bearing fix; the first is hygiene (avoids registering a useless drop target on macOS). **MacPorts gotcha**: `port install` is a meta-target that only re-runs phases when no registry image exists. To force a rebuild after editing the work-tree source, you MUST `port -f uninstall <port> @<version>+<variants>` first — otherwise `port install` short-circuits to "already installed, just activate" and silently re-activates the previous (unpatched) image. The full migration scripts (`upgrade-gtk4-devel.sh`, `patch-gtk4-rootwindow.sh`) live in `~/macports-patches/`. **Rule**: when a Rust GUI crash log shows `__rust_foreign_exception` with no other context on macOS, the cause is almost always an ObjC `NSException` from AppKit/CoreFoundation — Rust cannot catch foreign exceptions, so the process aborts with no panic message. Look for AppKit `*** Terminating app due to uncaught exception` lines in the terminal output (NOT in the crash log); they carry the actual reason.

## Compact Instructions

When summarizing the conversation, focus on:
- Code changes with full file paths and snippets
- Bug fixes: what broke, root cause, how it was fixed
- Test names and what they cover
- Architectural decisions and trade-offs
- User preferences and explicit requests

## Commands

Debug builds are OK again. We use a custom cargo fork
(`github.com/baslr/cargo`, installed in PATH ahead of system cargo)
that auto-cleans `target/`, so the historic 25-30 GB debug-build bloat
(GTK4 hash-versioned rebuilds + 8 GB incremental cache) is no longer
an issue. Pass `--release` only when you actually want optimized
binaries (benchmarks, shipping).

```bash
cargo t                         # Run all tests (= test --workspace, debug)
cargo b                         # Build (debug)
cargo r                         # Run TUI (debug)
cargo t --release               # Release tests when explicitly needed
cargo r --release -- --resume   # Resume last saved session, optimized
cargo r -- --print "prompt"     # Headless mode
```

The aliases are defined in `.cargo/config.toml`.

### TUI Commands

| Command | Effect |
|---------|--------|
| `/help`, `/?` | Show available commands and keybindings |
| `/clear`, `/reset`, `/new` | Clear conversation, start fresh |
| `/unwind [N]` | Roll back the last N text messages (default 1; tool round-trips removed together) |
| `/compact [instructions]` | Compact conversation history |
| `/sessions` | List all saved sessions |
| `/resume` | Load the most recent saved session |
| `/resume <prefix>` | Load session matching prefix (e.g. `2026_04_13`, `2026_04_13_14`) |
| `/stop`, `stop` | Cooperatively interrupt the agentic turn loop (also: double-Esc while loading) |
| `! <cmd>` | Execute shell command |
| `exit`, `quit`, `/exit`, `/quit` | Exit |

### TUI Key Bindings

| Key | Effect |
|-----|--------|
| `Enter` | Submit input |
| `Ctrl+J` | Insert newline (works in ALL terminals) |
| `Shift+Enter` or `Alt+Enter` | Insert newline (Kitty protocol terminals only) |
| `Alt+b` / `Alt+f` | Move cursor word left / right |
| `Alt+d` | Delete word forward |
| `Up` / `Down` | Move cursor vertically in multiline input (falls through to history/tab bar on first/last line) |
| `Ctrl+K` | Delete word backward (right-hand home row) |
| `Ctrl+L` | Delete word forward (right-hand home row) |
| `Ctrl+c` | Quit |

### UserQuestion Dialog Keys

| Key | Effect |
|-----|--------|
| `↑` / `↓` | Navigate options |
| `Tab` | Toggle between options and free-text input |
| `1`-`9` | Quick-select option by number |
| `Enter` | Submit selected option or typed text |
| `Esc` | Cancel (sends empty string) |

## Session Persistence

Sessions are automatically saved to `~/.chlodwig-rs/session.json` after:
- Every completed conversation turn (`TurnComplete`)
- Every completed compaction (`CompactionComplete`)
- Every `/clear` (saves empty session)

The save is **asynchronous** (via `tokio::spawn` + `spawn_blocking`) so it never blocks the event loop. Writes use atomic rename (write to `.tmp`, then rename) to prevent corruption on crash.

**Resume**: Use `--resume` CLI flag or `/resume` TUI command. When resuming, only messages are restored — model, tools, and system prompt use the current CLI settings (they may have changed between sessions). Use `/resume <prefix>` to load a specific session by timestamp prefix (e.g. `/resume 2026_04_13`). Use `/sessions` to list all available sessions.

**Implementation**: `chlodwig-core/src/session.rs` — `SessionSnapshot`, `save_session()`, `load_session()`, `session_path()`.

## Self-Improvement Protocol

After every session, if bugs were found or patterns emerged:
1. Add the finding to "Known Gotchas" above
2. Ensure a regression test exists
3. Update any affected architectural notes

## Documentation

- **Keep `README.md` up to date.** When adding features, changing the project structure, modifying the pipeline, or altering the tech stack, update the root `README.md` accordingly. It is the primary entry point for anyone looking at this project.

## Tool Limitations

- **WebSearch & WebFetch available** — WebSearch uses Startpage (Google proxy) as primary backend with DuckDuckGo HTML as fallback. No API key needed. WebFetch retrieves any URL with automatic HTML-to-text conversion.
