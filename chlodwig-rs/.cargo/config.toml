# Project-local Cargo aliases.
#
# Debug builds are fine again: we use a custom cargo fork
# (github.com/baslr/cargo) that auto-cleans target/ so the historic
# 25-30 GB bloat from debug GTK4 rebuilds is no longer an issue.
[alias]
b = "build"
t = "test --workspace"
r = "run"
